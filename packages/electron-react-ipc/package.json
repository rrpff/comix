{
  "name": "electron-react-ipc",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc --declaration --project . --outDir .",
    "build:watch": "tsc --watch --declaration --project . --outDir .",
    "test": "jest"
  },
  "devDependencies": {
    "@types/uuid": "^8.3.0",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.5",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "uuid": "^8.3.2"
  }
}
