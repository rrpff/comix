{
  "name": "@comix/library-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "start": "electron .",
    "build": "tsc --project . --outDir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "electron": "^12.0.5",
    "typescript": "^4.2.4"
  }
}
