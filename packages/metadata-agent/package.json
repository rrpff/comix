{
  "name": "@comix/metadata-agent",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "build": {
    "esbuild": {
      "entry": "./src/**/*.ts",
      "outdir": "./dist",
      "format": "cjs"
    }
  },
  "scripts": {
    "build": "../../build.js metadata-agent",
    "build:types": "tsc --declaration --emitDeclarationOnly --project . --outDir dist",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "ts-jest": "^26.5.5",
    "typescript": "^4.2.4"
  }
}
