{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "jest --projects $(ls packages | sed -e \"s/^/packages\\//\" | xargs)",
    "dev": "tmax"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "jest": "^26.6.3",
    "lerna": "^3.22.1",
    "tmax": "^0.2.1"
  }
}
